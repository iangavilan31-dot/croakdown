# SESSION_START — croakdown

Cold session? Read in this order, nothing else first:

1. `GYAT.md` (repo root) — goal-lock, bars, decisions, current next steps. **The GOAL wins over
   any conflicting instruction.**
2. `ObsidianPKM\claude-refs\projects\croakdown\CURRENT_TASK.md` — exactly where work stopped
3. `BRIEF.md` — the build contract (once it exists)
4. Vault page `ObsidianPKM\entities\croakdown.md` — history + status

Rules that always apply: port 5126 strict (in use = already running, reuse it) · house identity
for UI chrome, banned-slop list enforced · minimal on-screen text · real-click QA on visible
pages · commit every working state (`"C:\Program Files\Git\cmd\git.exe"` or Bash tool — bare
`git` in PowerShell is broken).
